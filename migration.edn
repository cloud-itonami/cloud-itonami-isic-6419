{:schema "etzhayyim.migration/v1" :source {:repository "etzhayyim/root" :revision "1316716c6b4bc3b1cba8c0188557b732ceffa054" :path "60-apps/etzhayyim-project-open-banking" :tree "425a363f8440167fed4ab551c4e962893658e9c5" :tracked-files 36 :bytes 113682} :destination {:repository "etzhayyim/com-etzhayyim-app-open-banking"} :identity {:allowed-additions ["README.edn" "migration.edn"]} :deviations [{:date "2026-08-26" :change "cljs-migration" :description "Frontend migrated from SvelteKit (worker/svelte/) to ClojureScript (shadow-cljs + reagent + re-frame + jp-go-dds), added at worker/cljs/. worker/svelte/ was deleted (git rm), so :tracked-files/:bytes above no longer describe the working tree as it stands after this commit -- they remain the honest record of what the original import actually copied. The one non-frontend file worker/svelte/ held, the SvelteKit XRPC server route (src/routes/xrpc/[...path]/+server.ts), was moved byte-for-byte (only a header comment prepended) to worker/src/xrpc-mcp-router-proxy.ts, not deleted, since it is backend code out of scope for a frontend migration."}]}
